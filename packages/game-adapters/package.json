{
  "name": "@mmbt/game-adapters",
  "version": "0.0.0",
  "private": true,
  "description": "Pure game adapter registry and validation helpers for the Multi-MOBA Esports Broadcast Toolkit.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo Game adapter registry package contains pure local registration helpers.",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@mmbt/shared-types": "workspace:*"
  }
}
