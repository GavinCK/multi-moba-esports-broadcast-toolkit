{
  "name": "@mmbt/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types workspace placeholder for the Multi-MOBA Esports Broadcast Toolkit.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo TQ-010 skeleton only - shared types are not implemented yet.",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo No shared types tests configured yet for the TQ-010 skeleton.",
    "lint": "echo No shared types lint configured yet for the TQ-010 skeleton."
  }
}
