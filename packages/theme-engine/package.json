{
  "name": "@mmbt/theme-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Pure theme defaults, validation, and local asset helpers for the Multi-MOBA Esports Broadcast Toolkit.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo Theme engine package contains pure theme config helpers.",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@mmbt/shared-types": "workspace:*"
  }
}
