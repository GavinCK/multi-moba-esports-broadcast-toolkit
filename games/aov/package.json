{
  "name": "@mmbt/game-aov",
  "version": "0.0.0",
  "private": true,
  "description": "AOV game adapter workspace placeholder.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo TQ-010 skeleton only - AOV adapter is not implemented yet.",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0"
  }
}
